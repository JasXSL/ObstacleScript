#ifndef __SlideBeam
#define __SlideBeam

// Desc: [(str)label]

#define SlideBeamConst$CHAN (OBSTACLE_CHAN+0x511)	// Listener

#define SlideBeamTask$vibrate 1						// (bool)on

#define SlideBeam$vibrate( label, on ) \
	llRegionSay(SlideBeamConst$CHAN, mkarr(label + SlideBeamTask$vibrate + (on)))



#endif
 

